apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alcachart.fullname" . }}-test-connection"
  labels:
    {{- include "alcachart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alcachart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
